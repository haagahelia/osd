<?xml version="1.0" encoding="UTF-8"?>
<?xml-model href="exercise.xsd"?>
<exercise id="07.05" title="Gross Salary" difficulty="medium"
	size="M"
	returnbox="http://hhmoodle.haaga-helia.fi/mod/assign/view.php?id=396505&amp;action=editsubmission"
	template="xc/07.05.html">

	<p class="exercise_text">
		Make a program that calculates the gross salary (bruttopalkka) based on the hours done
		and the hourly salary. Define a salary calculation function, which
		receives the hours and hourly salary as parameters and returns the
		gross salary.
	</p>
	<p class="exercise_gui_title">Sample output for shown input:</p>
	<p>
		<img src="xc/07.05.png" class="bordered" />
	</p>
	<p>HINTS:</p>
	<p>
		The idea of the code is this
		<ul>
			<li><pre><code>function calculateSalary(hours, hourlySalary) {
		// calculate salary
		<br />
		// return the gross salary with the return statement
}</code></pre>
			</li>
			<li><pre><code>function showSalary() {
		// assign the input field value to the variable hours
		// assign the second input field value to the variable hourlySalary
		// call the calculateSalary function
		// write the answer on the html page
}</code></pre>
			</li>
			<li>
				Call the showSalary() function from the Calculate button’s onclick
				event.
			</li>
		</ul>
	</p>

	<p>Submit a link to your code (myy or jsbin). </p>

</exercise>